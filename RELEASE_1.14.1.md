# IronCycle 1.14.1

Fixes the Android Quick Add custom-exercise NameError by using a package-safe local dictionary lookup. Adds the corresponding database API and strengthens startup symbol validation. Schema remains 11. This package is cumulative over 1.14.0.
